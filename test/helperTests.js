var assert = chai.assert;

describe("dropDownSelector", function(){
  it("should return SOMETHING if ID exists");
  it("should return a proper error if ID does not exist");
  it("should return a proper error if ID is not a string");
});
